# cmd/wallet-provider output: the private key lets anyone attest wallets
# the demo issuer accepts.
wallet-provider.pem
wallet-provider.jwks.json

# cmd/issuer's generated TLS certificate and cmd/wallet's credential
# store (credentials and their holder keys, unencrypted).
issuer-tls.pem
issuer-ca.pem
verifier-tls.pem
wallet-store/
